1. API를 만들때에는 항상 엔티티를 파라미터로 받으면 안된다.
    - 엔티티를 외부에 노출하면 안된다.

2. 조회 V1 : 응답 값으로 엔티티를 직접 외부에 노출한 조회
    - 기본적으로 엔티티 모든 값들이 노출
    - 엔티티가 변경되면 API스펙이 변경됨

<선호 방법> : API를 만들때 절대 엔티티를 노출하지 말아라.
            중간에 API스펙에 맞는 Dto를 만들어서 처리해라.